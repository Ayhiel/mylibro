export const Button = ({children}) => {
  return (
    <button className="ring-1 ring-yellow-500 md:text-yellow-500 text-xs px-8 py-4 mt-8 rounded-full hover:ring-gray-200 hover:text-gray-200">{children}</button>
  )
}
